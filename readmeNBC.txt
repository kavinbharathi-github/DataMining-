Steps for the execution of the code:

1.)Install the required Python libraries: pandas and numpy. You can use pip command to install them:
Copy code
pip install pandas numpy
2.) Copy the code into a new Python file, e.g. rt_reviews.py.
3.)Run the Python file using a Python interpreter, e.g. from the command line:
python rt_reviews.py
4.)After running the code, you can use the following commands to check the contents of the datasets:
4a.)To print the train_df dataset:
print(train_df)
4b.)To print the dev_df dataset:
print(dev_df)
4c.)To print the test_df dataset:
print(test_df)
5.)The output will be printed in the console or command prompt, depending on how you run the Python file.